{% extends 'base.html.twig' %}

{% block title %}Modifier mon profil créateur{% endblock %}

{% block stylesheets %}
    {{ parent() }}    <style>
        /* Style du conteneur principal */
        .select2-container--default .select2-selection--multiple {
            background-color: white;
            border: 1px solid #d1d5db;
            border-radius: 0.375rem;
            min-height: 38px;
            padding: 2px;
        }

        /* Style des tags */
        .select2-container--default .select2-selection--multiple .select2-selection__choice {
            background-color: #e5e7eb;
            border: none !important;
            border-radius: 0.375rem;
            margin: 3px;
            padding: 2px 6px 2px 25px;
            font-size: 0.875rem;
            line-height: 1.5;
            position: relative;
        }

        /* Style du texte dans les tags */
        .select2-container--default .select2-selection--multiple .select2-selection__choice__display {
            padding: 0;
            cursor: default;
            user-select: none;
        }

        /* Style du bouton de suppression */
        .select2-container--default .select2-selection--multiple .select2-selection__choice__remove {
            position: absolute;
            left: 4px;
            top: 50%;
            transform: translateY(-50%);
            border-radius: 50%;
            border: none !important;
            background: none;
            color: #6b7280;
            font-size: 14px;
            font-weight: 600;
            padding: 0 4px;
            margin: 0;
            cursor: pointer;
            transition: all 0.2s;
        }

        /* Hover sur le bouton de suppression */
        .select2-container--default .select2-selection--multiple .select2-selection__choice__remove:hover {
            background-color: #d1d5db;
            color: #374151;
        }

        /* Style de la zone de recherche */
        .select2-container--default .select2-selection--multiple .select2-search--inline {
            margin: 3px;
        }

        .select2-container--default .select2-selection--multiple .select2-search--inline .select2-search__field {
            margin: 0;
            padding: 0 6px;
            height: 28px;
            min-width: 150px;
        }
    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        // Initialisation de Select2
        document.addEventListener('DOMContentLoaded', function() {
            if (typeof $ !== 'undefined') {
                $('#{{ form.categories.vars.id }}').select2({
                    theme: 'default',
                    width: '100%',
                    placeholder: 'Rechercher des catégories...',
                    allowClear: true,
                    multiple: true,
                    language: 'fr',
                    closeOnSelect: false
                });
            }
        });

        // Code de image-preview.js directement ici
        document.addEventListener('DOMContentLoaded', function() {
            const fileInput = document.querySelector('input[type="file"]');
            const imagePreview = document.querySelector('.image-preview');
            const placeholder = document.querySelector('.image-preview-placeholder');
            const uploadStatus = document.querySelector('.upload-status');
            const deleteButton = document.querySelector('.delete-image-btn');
            const deleteInput = document.querySelector('.delete-image-input');

            if (fileInput) {
                fileInput.addEventListener('change', function() {
                    if (this.files && this.files[0]) {
                        const reader = new FileReader();
                        reader.onload = function(e) {
                            imagePreview.src = e.target.result;
                            imagePreview.classList.remove('hidden');
                            placeholder.classList.add('hidden');
                            uploadStatus.classList.remove('hidden');
                        }
                        reader.readAsDataURL(this.files[0]);
                    }
                });
            }

            if (deleteButton && deleteInput) {
                deleteButton.addEventListener('click', function() {
                    deleteInput.checked = true;
                    imagePreview.src = '';
                    imagePreview.classList.add('hidden');
                    placeholder.classList.remove('hidden');
                    if (fileInput) fileInput.value = '';
                });
            }
        });

        // Code de tabs.js directement ici
        document.addEventListener('DOMContentLoaded', function() {
            const tabButtons = document.querySelectorAll('.tab-button');
            const tabContents = document.querySelectorAll('.tab-content');

            function switchTab(tabId) {
                tabContents.forEach(content => {
                    content.classList.add('hidden');
                });

                tabButtons.forEach(button => {
                    button.classList.remove('border-primary', 'text-primary');
                    button.classList.add('border-transparent', 'text-gray-500');
                });

                const selectedContent = document.getElementById(tabId + '-content');
                if (selectedContent) {
                    selectedContent.classList.remove('hidden');
                }

                const selectedButton = document.querySelector(`[data-tab="${tabId}"]`);
                if (selectedButton) {
                    selectedButton.classList.remove('border-transparent', 'text-gray-500');
                    selectedButton.classList.add('border-primary', 'text-primary');
                }
            }

            tabButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const tabId = button.getAttribute('data-tab');
                    switchTab(tabId);
                });
            });
        });
    </script>
{% endblock %}

{% block body %}
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="bg-white shadow-xl rounded-lg overflow-hidden">
            <div class="px-4 py-5 sm:p-6">
                <h1 class="text-2xl font-semibold text-gray-900 mb-6">Modifier mon profil créateur</h1>

                {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                    <div class="space-y-6 sm:space-y-5">
                        {# Nom public #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.displayName.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.displayName) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.displayName, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:max-w-xs sm:text-sm border-gray-300 rounded-md'}}) }}
                                {{ form_errors(form.displayName) }}
                            </div>
                        </div>

                        {# Description #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.description.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.description) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.description, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm border-gray-300 rounded-md', 'rows': '4'}}) }}
                                {{ form_errors(form.description) }}
                            </div>
                        </div>

                        {# Site web #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.website.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.website) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.website, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:max-w-xs sm:text-sm border-gray-300 rounded-md'}}) }}
                                {{ form_errors(form.website) }}
                            </div>
                        </div>

                        {# Profil Instagram #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.instagramProfile.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.instagramProfile) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.instagramProfile, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:max-w-xs sm:text-sm border-gray-300 rounded-md'}}) }}
                                {{ form_errors(form.instagramProfile) }}
                            </div>
                        </div>

                        {# Profil Facebook #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.facebookProfile.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.facebookProfile) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.facebookProfile, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:max-w-xs sm:text-sm border-gray-300 rounded-md'}}) }}
                                {{ form_errors(form.facebookProfile) }}
                            </div>
                        </div>

                        {# Profil Pinterest #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.pinterestProfile.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.pinterestProfile) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.pinterestProfile, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:max-w-xs sm:text-sm border-gray-300 rounded-md'}}) }}
                                {{ form_errors(form.pinterestProfile) }}
                            </div>
                        </div>

                        {# Catégories #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.categories.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.categories) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.categories, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:max-w-xs sm:text-sm border-gray-300 rounded-md select2'}}) }}
                                {{ form_errors(form.categories) }}
                                <p class="mt-2 text-sm text-gray-500">Sélectionnez les catégories qui correspondent à vos créations.</p>
                            </div>
                        </div>

                        {# Informations pratiques #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.practicalInfos.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.practicalInfos) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">
                                {{ form_widget(form.practicalInfos, {'attr': {'class': 'max-w-lg block w-full shadow-sm focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm border-gray-300 rounded-md', 'rows': '4'}}) }}
                                {{ form_errors(form.practicalInfos) }}
                            </div>
                        </div>

                        {# Image de couverture #}
                        <div class="sm:grid sm:grid-cols-3 sm:gap-4 sm:items-start">
                            <label for="{{ form.coverImage.vars.id }}" class="block text-sm font-medium text-gray-700 sm:mt-px sm:pt-2">
                                {{ form_label(form.coverImage) }}
                            </label>
                            <div class="mt-1 sm:mt-0 sm:col-span-2">                                <div class="max-w-lg flex justify-center px-6 pt-5 pb-6 border-2 border-gray-300 border-dashed rounded-md image-preview-container">
                                    <div class="space-y-1 text-center">
                                        <div class="flex flex-col items-center">
                                            <div class="relative w-32 h-32 mb-4">                                            {% if creator.creatorInfo.coverImage %}
                                                <div class="relative group">
                                                    <img src="{{ asset('uploads/images/' ~ creator.creatorInfo.coverImage) }}" 
                                                         alt="Image actuelle" 
                                                         class="w-full h-full object-cover rounded-lg image-preview">
                                                    <button type="button" 
                                                            class="absolute top-0 right-0 m-2 p-1 bg-red-500 text-white rounded-full opacity-0 group-hover:opacity-100 transition-opacity delete-image-btn"
                                                            title="Supprimer l'image">
                                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                                                        </svg>
                                                    </button>
                                                    {{ form_widget(form.deleteCoverImage, {'attr': {'class': 'hidden delete-image-input'}}) }}
                                                </div>
                                            {% else %}
                                                <img src="" alt="" class="w-full h-full object-cover rounded-lg hidden image-preview">
                                            {% endif %}
                                                <div class="image-preview-placeholder {% if creator.creatorInfo.coverImage %}hidden{% endif %} w-full h-full rounded-lg bg-gray-100 flex items-center justify-center">
                                                    <svg class="h-12 w-12 text-gray-400" stroke="currentColor" fill="none" viewBox="0 0 48 48">
                                                        <path d="M28 8H12a4 4 0 00-4 4v20m32-12v8m0 0v8a4 4 0 01-4 4H12a4 4 0 01-4-4v-4m32-4l-3.172-3.172a4 4 0 00-5.656 0L28 28M8 32l9.172-9.172a4 4 0 015.656 0L28 28m0 0l4 4m4-24h8m-4-4v8m-12 4h.02" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                                                    </svg>
                                                </div>
                                            </div>
                                            <div class="mt-2 text-center">
                                                <div class="flex text-sm text-gray-600 justify-center">
                                                    <label for="{{ form.coverImage.vars.id }}" class="relative cursor-pointer bg-white rounded-md font-medium text-indigo-600 hover:text-indigo-500 focus-within:outline-none focus-within:ring-2 focus-within:ring-offset-2 focus-within-ring-indigo-500">
                                                        {{ form_widget(form.coverImage, {'attr': {
                                                            'class': 'sr-only',
                                                            'data-preview-target': 'image-preview'
                                                        }}) }}
                                                        <span>Télécharger un fichier</span>
                                                    </label>
                                                    <p class="pl-1">ou glisser-déposer</p>
                                                </div>
                                                <p class="text-xs text-gray-500 mt-2">PNG, JPG jusqu'à 5MB</p>
                                                <p class="text-xs text-indigo-600 mt-2 upload-status hidden">Image sélectionnée</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                {{ form_errors(form.coverImage) }}
                            </div>
                        </div>
                    </div>

                    <div class="pt-5">
                        <div class="flex justify-end">
                            <a href="{{ path('app_creator_show', {'id': creator.id}) }}" class="bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                                Annuler
                            </a>
                            <button type="submit" class="ml-3 inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                                Enregistrer
                            </button>
                        </div>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
