{{ form_start(form, {'attr': {'class': 'flex items-center'}}) }}
    <div class="relative">
        {{ form_widget(form.query, {
            'attr': {
                'class': 'w-64 bg-white/10 text-white placeholder-white/70 border border-white/20 rounded-md py-2 px-3 focus:outline-none focus:ring-2 focus:ring-white/50 focus:border-transparent',
                'placeholder': 'Rechercher...'
            }
        }) }}
        <button type="submit" class="absolute right-0 top-0 h-full px-3 text-white hover:text-gray-200">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
        </button>
    </div>
{{ form_end(form) }} 